{
  "description": "Universal AI Dev Kit lifecycle hooks (macOS native)",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "replace_file_content|write_to_file|multi_replace_file_content|Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "ruff check --fix . 2>/dev/null || true; ruff format . 2>/dev/null || true",
            "timeout": 30
          },
          {
            "type": "command",
            "command": "npx -y prettier --write '**/*.{js,jsx,ts,tsx,json,css,scss,md}' --ignore-unknown 2>/dev/null || true",
            "timeout": 30
          },
          {
            "type": "command",
            "command": "gofmt -w . 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ],
    "Stop": [
      {
        "type": "command",
        "command": "osascript -e 'display notification \"AI task completed!\" with title \"AI Dev Kit\"' && afplay /System/Library/Sounds/Glass.aiff 2>/dev/null || true",
        "timeout": 10
      }
    ]
  }
}
